<% if obj.errors.any? %>
    <div class="row">
        <div class="col-12">
            <div class="alert alert-danger ">
                <div class="panel-body">
                    <h5 class="alert-heading"><%= pluralize(obj.errors.count, "error") %> prohibited this from being saved:</h5>
                    <ul>
                      <% obj.errors.full_messages.each do |msg| %>
                          <li>
                              <%= msg %>
                          </li>
                        <% end %>
                    </ul>
                </div>
            </div>
        </div>
    </div> 
<% end %>